input {
    keyboard {
        xkb {
            layout "us"
            options "caps:escape"
        }
        repeat-delay 250
        repeat-rate 50
        numlock
    }
    touchpad {
        natural-scroll
    }
    focus-follows-mouse max-scroll-amount="10%"
}

output "DP-3" {
    variable-refresh-rate
    mode "2560x1440@170.071"
}

hotkey-overlay {
    skip-at-startup
}

prefer-no-csd

debug {
    honor-xdg-activation-with-invalid-serial
}

gestures {
    hot-corners {
        off
    }
}

blur {
  passes 2
  offset 3.0
  noise 0.03
  saturation 1.0
}

window-rule {
    geometry-corner-radius 16
    clip-to-geometry true
    draw-border-with-background false
    opacity 0.9
    background-effect {
        blur true
        xray true
    }
}

window-rule {
    match app-id="^equibop$"
    opacity 1.0
}

window-rule {
    match app-id="^equibop$" title="^([^E]|E[^q]|Eq[^u]|Equ[^i]|Equi[^b]|Equib[^o]|Equibo[^p])*$"
    open-floating true
    default-column-width { fixed 888; }
    default-window-height { fixed 499; }
    default-floating-position x=0 y=0 relative-to="bottom-right"
}

window-rule {
    match title="^Picture in picture$"
    opacity 1.0
    open-floating true
    default-column-width { fixed 826; }
    default-window-height { fixed 413; }
    default-floating-position x=32 y=32 relative-to="bottom-right"
}

layer-rule {
  match namespace="^noctalia-wallpaper"
  place-within-backdrop true
}

layout {
  background-color "transparent"
  //gaps 0
  focus-ring {
    on
  }
  insert-hint {
      off
  }
  preset-column-widths {
      proportion 0.33333
      proportion 0.5
      proportion 0.66667
      fixed 1280
  }
}

overview {
  workspace-shadow {
    off
    }
}

layer-rule {
  match namespace="^noctalia-(bar-main|notification|dock|panel)$"
  background-effect {
    xray false
    }
}

binds {
    Mod+Return { spawn "ghostty"; }
    Mod+B      { spawn "helium"; }
    Mod+D      { spawn "equibop"; }
    Mod+Z      { spawn "zeditor"; }

    Mod+Escape { spawn "noctalia" "msg" "panel-toggle" "session"; }
    Mod+C      { spawn "noctalia" "msg" "panel-toggle" "control-center"; }
    Mod+S      { spawn "noctalia" "msg" "panel-toggle" "launcher"; }
    Mod+V      { spawn "noctalia" "msg" "panel-toggle" "clipboard"; }

    Mod+Shift+C { set-dynamic-cast-window; }
    Mod+Ctrl+C  { set-dynamic-cast-monitor; }

    XF86AudioRaiseVolume { spawn "noctalia" "msg" "volume-up"; }
    XF86AudioLowerVolume { spawn "noctalia" "msg" "volume-down"; }
    XF86AudioMute        { spawn "noctalia" "msg" "volume-mute"; }

    Mod+Q       { close-window; }
    Mod+F       { maximize-column; }
    Mod+Shift+F { toggle-window-rule-opacity; }
    Mod+T       { toggle-window-floating; }
    Mod+R       { switch-preset-column-width; }
    Mod+Tab     { toggle-overview; }
    Print       { screenshot; }

    Mod+H { focus-column-left; }
    Mod+L { focus-column-right; }
    Mod+J { focus-workspace-down; }
    Mod+K { focus-workspace-up; }

    Mod+Shift+H { move-column-left; }
    Mod+Shift+L { move-column-right; }
    Mod+Shift+J { move-column-to-workspace-down; }
    Mod+Shift+K { move-column-to-workspace-up; }

    Mod+WheelScrollUp         { focus-workspace-up; }
    Mod+WheelScrollDown       { focus-workspace-down; }
    Mod+Shift+WheelScrollUp   { focus-column-left; }
    Mod+Shift+WheelScrollDown { focus-column-right; }
}

spawn-at-startup "dbus-update-activation-environment" "--systemd" "WAYLAND_DISPLAY" "XDG_CURRENT_DESKTOP=niri"
spawn-at-startup "noctalia"
spawn-at-startup "bash" "-c" "screencast-monitor"
spawn-at-startup "bash" "-c" "tile-to-2"
spawn-at-startup "bash" "-c" "niri-float-sticky -title '^Picture in picture$'"
spawn-at-startup "bash" "-c" "niri-float-sticky -app-id '^equibop$' -title '^([^E|]|E[^q|]|Eq[^u|]|Equ[^i|]|Equi[^b|]|Equib[^o|]|Equibo[^p|]|Equibop[^$|])[^|]*$'"
